Описание проекта:

    Не привязанно к проекту:
        папка db содержит файлы работы с бд(не асинхронно)
        папка gui содержит файлы графической оболочки
        файл server_n.py имеет более расширенную работу с проверкой входящих сообщений и обработку различных типов
        сообщений но не адаптирован в текущий проект. часть функций будут адаптироваться в сервер написанный на асинке.
        a_server.py теоретически асинхронный сервер, проверить не вариант, буду переделывать полностью, так в продакшен пускать нельзя
        по словам кор разработчиков асинка если использовать высокий уровень абстракции модуля асинкио то надо пользоваться функцией
        asyncio.run() об этой функции нам рассказать на уроке отказались, я сам её изучил, но на ноуте проблема с питон3.7, перезалью систему и реализую сервер.

    Связанно в проект:
        client.py   клиент. работает на потоках, в данной реализации захардкожен ник Алекс, будет переписан на асинк
        server.py   сервер. так же на потоках, будет переписан на асинк с дописыванием функций обработки всех типов сообщений и позволяющий подключить киви и веб клиентов
        jim.py      общий для сервера и клиента файл с вынесными функциями. Будет дополняться.
        type_msg.py файл с функциями всех типов сообщений. как по мне так самая жёсткая и статичная реализация протокола jim. будет расширятся под особенности веб и мобильного клиента.
